# объявление функции
def get_factors(num):
    spisok = []
    for i in range(1, num+1):
        if num%i ==0:
            spisok.append(i)
    return spisok

# считываем данные
n = int(input())

# вызываем функцию
print(get_factors(n))
